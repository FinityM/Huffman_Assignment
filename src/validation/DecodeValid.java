package validation;

public class DecodeValid {
    public void validateDigits(Object number) {
        // Can only take 9 binary digits, anymore is not allowed
        // Can only be 1 or 0
    }

}
